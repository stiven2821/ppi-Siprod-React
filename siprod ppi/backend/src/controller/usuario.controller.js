const DataBase = require("../database");

const usuarioCtrl = {};

usuarioCtrl.crearUsuario = async(req, res) => {
    try {
        const query =
        ''
        
    } catch (error) {
        
    }
    
}
